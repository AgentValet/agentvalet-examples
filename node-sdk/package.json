{
  "name": "agentvalet-example-node-sdk",
  "private": true,
  "type": "module",
  "description": "Governed platform access from a plain Node agent \u2014 no harness, no framework.",
  "scripts": {
    "build": "tsc",
    "demo": "npm run build && node dist/demo.js",
    "smoke": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@agentvalet/client": "0.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
